{
  "name": "newhope-chat",
  "version": "2.0.0",
  "private": true,
  "description": "NEWHOPE-CHAT - React + Express rebuild",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server && npm install --prefix functions",
    "deploy:functions": "firebase deploy --only functions"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
